var defaultValues = {
    flexDirection: "row",
    alignItems: "stretch",
    justifyContent: "flex-start"
};
function solution(root, css) {
    var res = [];
    if (!css) {
        res.push({
            x: 0, y: 0, width: root.style.width, height: root.style.height
        });
    }
    root.children.forEach(function (c) {
        c.style.
        ;
    });
}
module.exports = function () {
};
